# Remote Crew

This page has moved to [remote-crew.md](remote-crew.md).

The filename was a published URL, so this stub stays behind to keep existing
links resolving — these docs are plain markdown served from the repository and
the packaged copy, with no site generator, so there is no true redirect
mechanism to point the old address at the new one.
